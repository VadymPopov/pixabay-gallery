const refs = {
  form: document.querySelector('.search-form'),
  gallery: document.querySelector('.gallery'),
  loader: document.querySelector('.loader'),
  preloaderWrapper: document.querySelector('.preloader-wrapper'),
};
const { form, gallery, loader, preloaderWrapper } = refs;

export { form, gallery, loader, preloaderWrapper };
